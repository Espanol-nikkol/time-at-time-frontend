export type Statistic = {
    startDate: string;
    endDate: string;
    restTime: number;
    productiveTime: number;
    countRecords: number;
    // Подобрать слово для "Серия" / may be strik
    length: number;
};
